task_id: TASK-BROWSER-RUNTIME-FIX-01
status: blocked
changed_files:
- C:\Coding\Naruto arena agent\.orchestrator\browser-runtime-fix-report.md
- C:\Coding\Naruto arena agent\.orchestrator\tasks\TASK-BROWSER-RUNTIME-FIX-01\RESULT.txt

validation:
- command: where.exe node
  result: found `C:\nvm4w\nodejs\node.exe` first and `C:\Program Files\WindowsApps\OpenAI.Codex_26.422.2437.0_x64__2p2nqsd0c76g0\app\resources\node.exe` second.
- command: node --version
  result: active shell/runtime version is `v20.19.5`.
- command: nvm list; nvm current
  result: installed versions are `20.19.5` active and `22.20.0` alternate; `22.20.0` is still below the required `>= v22.22.0`.
- command: inspect Env:NODE_REPL_NODE_PATH
  result: variable is not set, so `node_repl` has no explicit runtime override.
- command: mcp__node_repl__.js direct repro
  result: failed with `Node runtime too old for node_repl (resolved C:\nvm4w\nodejs\node.exe): found v20.19.5, requires >= v22.22.0. Install/update Node or set NODE_REPL_NODE_PATH to a newer runtime.`
- command: direct execution of Codex bundled node.exe; file metadata inspection
  result: bundled runtime exists and reports file/product version `24.14.0`, but direct execution from this worker returned `Access is denied`, so it is not a validated fallback path here.
- command: write-scope inspection
  result: changed files are limited to the declared write_scope.

completed_acceptance:
- Determined why `node_repl` resolves `v20.19.5`: `NODE_REPL_NODE_PATH` is unset and the active `nvm` shim `C:\nvm4w\nodejs\node.exe` points to `20.19.5`.
- Determined that switching to the already installed `22.20.0` would still not satisfy Browser Use because the minimum is `>= v22.22.0`.
- Provided the least risky fix path: use the existing `nvm` workflow to install and activate `22.22.0` or newer rather than relying on the WindowsApps bundled runtime.
- Documented the exact user-approved commands needed.

limitations:
- No runtime/system change was applied because installing Node, switching global `nvm` version, or setting `NODE_REPL_NODE_PATH` requires explicit user confirmation at action time and that authorization was not present in this worker thread.
- Browser Use bootstrap could not be verified as successful because the compatible runtime was not activated during this task.
- The WindowsApps bundled Node `24.14.0` was visible by path and metadata, but could not be validated as executable from this worker because direct execution returned `Access is denied`.

follow_up_needed: yes
follow_up:
- User-authorized fix path:
  `nvm install 22.22.0`
  `nvm use 22.22.0`
  `node --version`
- If `node_repl` still resolves the old version after `nvm use`, apply an explicit runtime override in a separately authorized step:
  `setx NODE_REPL_NODE_PATH "C:\Users\user\AppData\Local\nvm\v22.22.0\node.exe"`
- After the approved runtime change, rerun a minimal Browser Use `iab` bootstrap check and then rerun browser reconnaissance tasks.
